---
phase: 05-html-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/docs.html
autonomous: true
requirements:
  - DOCS-02
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "HTML template file exists at templates/docs.html with complete page structure"
    - "Template contains Getting Started section with 3-step workflow (tokenize, send to Claude, detokenize)"
    - "Template contains Token Categories table with all 19 categories"
    - "Template has copy-to-clipboard JS with navigator.clipboard + execCommand fallback"
    - "Template uses dark theme matching CLI colors (#1a1a2e background, yellow headers, green code, cyan accents)"
    - "Template has collapsible sidebar with hamburger toggle for mobile"
    - "All CSS and JS are embedded inline -- no external references"
  artifacts:
    - path: "templates/docs.html"
      provides: "Complete Askama HTML template with embedded CSS and JS"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "templates/docs.html"
      to: "src/docs.rs (Plan 02)"
      via: "Askama #[template(path = 'docs.html')] derive macro"
      pattern: "Askama template variables like {{ version }}, {% for cmd in commands %}"
---

<objective>
Create the Askama HTML template file that will be compiled into the logtok binary for documentation generation.

Purpose: This template defines the entire visual design, content structure, sidebar navigation, copy-to-clipboard functionality, and responsive layout. It is the single artifact that determines what `logtok docs` produces.

Output: `templates/docs.html` -- a complete Askama/Jinja2-syntax HTML template with embedded CSS and JS, ready to be rendered by the docs.rs module in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-html-documentation/05-CONTEXT.md
@.planning/phases/05-html-documentation/05-RESEARCH.md

Source files for reference:
@src/cli.rs (CLI structure -- source of truth for command names, flags, help text patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Askama HTML template with full design, content, and interactivity</name>
  <files>templates/docs.html</files>
  <read_first>
    - .planning/phases/05-html-documentation/05-CONTEXT.md (all D-01 through D-11 decisions)
    - .planning/phases/05-html-documentation/05-RESEARCH.md (Askama template syntax, copy-to-clipboard JS pattern, pitfalls)
    - .planning/phases/04-colored-cli-help/04-CONTEXT.md (color palette: yellow headers, green flags, cyan usage -- D-01 through D-03)
    - src/cli.rs (current CLI structure to understand what command data will look like)
    - CLAUDE.md (token categories table with all 19 categories and descriptions)
  </read_first>
  <action>
Create `templates/docs.html` -- a complete Askama template file. This is a large single file containing HTML structure, embedded CSS in a `<style>` tag, and embedded JS in a `<script>` tag.

**Template Variables (Askama syntax):**
The template will receive these variables from the Rust DocsTemplate struct (built in Plan 02):
- `{{ version }}` -- crate version string (e.g., "0.1.0")
- `{% for cmd in commands %}` -- Vec of CommandInfo structs with fields:
  - `{{ cmd.name }}` -- subcommand name (e.g., "tokenize")
  - `{{ cmd.about }}` -- short description
  - `{{ cmd.long_about }}` -- optional longer description (use `{% if let Some(ref long) = cmd.long_about %}`)
  - `{{ cmd.after_long_help }}` -- optional examples text (use `{% if let Some(ref help) = cmd.after_long_help %}`)
  - `{% for arg in cmd.args %}` -- Vec of ArgInfo with:
    - `{{ arg.name }}` -- arg identifier
    - `{{ arg.short }}` -- Option char (use `{% if let Some(s) = arg.short %}`)
    - `{{ arg.long }}` -- Option String (use `{% if let Some(ref l) = arg.long %}`)
    - `{{ arg.help }}` -- description string
    - `{{ arg.required }}` -- bool
    - `{{ arg.default_value }}` -- Option String
- `{% for cat in token_categories %}` -- Vec of TokenCategory structs:
  - `{{ cat.prefix }}` -- e.g., "IP"
  - `{{ cat.description }}` -- e.g., "IP addresses (v4 and v6)"
- `{% for garg in global_args %}` -- Vec of ArgInfo for global flags (--quiet, --config)

**Page Structure (per D-04):**

1. **Sidebar Navigation (per D-02, D-05):**
   - Fixed position on left, 260px wide
   - Visible by default on desktop (>768px), hidden on mobile
   - Hamburger button (three-line icon using CSS `span` elements, no SVG/icon font) toggles sidebar on mobile
   - Contains links: Getting Started, Overview, Commands (with nested links for each subcommand), Token Categories
   - Active section highlighting via scroll spy is NOT required (discretion: skip for simplicity)
   - Sidebar links use `<a href="#section-id">` anchors

2. **Getting Started Section (per D-04, for DOCS-02):**
   - Install instructions: `cargo install logtok` (in a code block with copy button)
   - Quick Start 3-step workflow:
     - Step 1: Tokenize -- `logtok tokenize server.log -o safe.log` (code block with copy button)
     - Step 2: Send `safe.log` to Claude Code for analysis (text description)
     - Step 3: De-tokenize -- `logtok detokenize response.txt` (code block with copy button)

3. **Overview Section:**
   - Brief description of what logtok does (tokenizes sensitive data for safe AI analysis)
   - Workflow diagram in text: Tokenize -> Analyze -> De-tokenize
   - Mention of encrypted token store, cross-platform support

4. **Command Reference Section (per D-04, D-08 structure):**
   - One subsection per subcommand (from `{% for cmd in commands %}`)
   - Each subsection: heading `logtok <name>`, description from `cmd.about`/`cmd.long_about`, flags table, examples from `cmd.after_long_help` in a code block with copy button
   - Global Options subsection showing --quiet and --config flags (from `global_args`)

5. **Token Categories Section (per D-04):**
   - Table with columns: Token Prefix, Represents
   - All 19 categories rendered from `token_categories` template variable
   - Brief intro text explaining the `[CATEGORY_NNN]` format

**CSS Design (per D-01, D-03, D-06):**

Dark theme colors (matching CLI STYLES const):
- Page background: `#1a1a2e`
- Sidebar background: `#16213e`
- Content area: `#1a1a2e`
- Section headers (h1, h2): `#f0c040` (yellow), font-weight: bold -- matches `AnsiColor::Yellow` + bold
- Code/command text, inline code: `#4ecca3` (green) -- matches `AnsiColor::Green`
- Links, accents: `#36d1dc` (cyan) -- matches `AnsiColor::Cyan`
- Body text: `#e0e0e0` (light gray)
- Code block background: `#242444` (slightly lighter than page bg, per D-03)
- Code block border: `1px solid #333366`
- Error/warning text: `#ff6b6b` (red) -- matches `AnsiColor::Red`

Typography:
- Body: `system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif` (system font stack, no web fonts per D-03)
- Code: `'Cascadia Code', 'Fira Code', 'JetBrains Mono', 'Consolas', 'Monaco', monospace`
- Base font size: 16px, line-height: 1.6

Layout:
- Content area: `max-width: 860px`, with left margin accommodating sidebar (260px + padding)
- Responsive: at <=768px, sidebar hidden, content full-width, hamburger visible
- Code blocks: `position: relative` wrapper, `overflow-x: auto`, `padding: 1rem`, `border-radius: 6px`
- Tables: full width, border-collapse, alternating row backgrounds (`#242444` / `#1e1e3a`)
- Flag tables: columns for Flag, Description, Default

**Copy-to-Clipboard JS (per D-09, D-10, D-11):**

Embed this JS in a `<script>` tag at end of `<body>`:

```javascript
function copyCode(btn) {
    var code = btn.parentElement.querySelector('code').textContent;
    if (navigator.clipboard && window.isSecureContext) {
        navigator.clipboard.writeText(code).then(function() {
            showCopied(btn);
        });
    } else {
        var textarea = document.createElement('textarea');
        textarea.value = code;
        textarea.style.position = 'fixed';
        textarea.style.opacity = '0';
        document.body.appendChild(textarea);
        textarea.select();
        document.execCommand('copy');
        document.body.removeChild(textarea);
        showCopied(btn);
    }
}

function showCopied(btn) {
    btn.textContent = '\u2713';
    btn.classList.add('copied');
    setTimeout(function() { btn.textContent = '\u{1F4CB}'; btn.classList.remove('copied'); }, 2000);
}
```

Copy button styling (per D-09):
- Position: `absolute`, top-right of code block wrapper
- Small button, semi-transparent background, cursor pointer
- `.copied` class turns button green briefly

Sidebar toggle JS:
- Hamburger button click toggles a class on sidebar element
- CSS transitions for smooth slide-in on mobile

**Code block macro pattern:**
Create a reusable pattern for code blocks with copy buttons. Each code block is wrapped:
```html
<div class="code-block">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy to clipboard">&#128203;</button>
  <pre><code>command here</code></pre>
</div>
```

**HTML structure:**
- `<!DOCTYPE html>` with `<html lang="en">`
- `<head>`: charset utf-8, viewport meta, title "logtok v{{ version }} Documentation"
- `<style>` tag with all CSS
- `<body>`: hamburger button, sidebar nav, main content area, `<script>` tag with JS
- Use semantic HTML: `<nav>`, `<main>`, `<section>`, `<h1>`-`<h3>`, `<table>`

**IMPORTANT Askama syntax notes:**
- Use `{% if let Some(ref x) = variable %}` for Option types
- Use `{% for item in collection %}` for iteration
- Use `{{ variable }}` for string interpolation (auto-escaped by default)
- Use `{{ variable|safe }}` ONLY for content that should render as raw HTML
- Askama 0.15 uses `{% if let %}` not `{% match %}`
  </action>
  <verify>
    <automated>
test -f templates/docs.html && echo "EXISTS" || echo "MISSING"
grep -c "DOCTYPE html" templates/docs.html
grep -c "#1a1a2e" templates/docs.html
grep -c "copyCode" templates/docs.html
grep -c "navigator.clipboard" templates/docs.html
grep -c "execCommand" templates/docs.html
grep -c "token_categories" templates/docs.html
grep -c "for cmd in commands" templates/docs.html
grep -c "768px" templates/docs.html
grep -c "Getting Started" templates/docs.html
    </automated>
  </verify>
  <acceptance_criteria>
    - templates/docs.html exists and starts with `<!DOCTYPE html>`
    - File contains CSS with `#1a1a2e` (dark background color)
    - File contains CSS with `#f0c040` or similar yellow for headers
    - File contains CSS with `#4ecca3` or similar green for code
    - File contains CSS with `#36d1dc` or similar cyan for links
    - File contains `copyCode` function in a `<script>` tag
    - File contains `navigator.clipboard.writeText` for modern browsers
    - File contains `execCommand('copy')` for file:// fallback
    - File contains `{% for cmd in commands %}` Askama loop for command reference
    - File contains `{% for cat in token_categories %}` Askama loop for categories
    - File contains `{% for garg in global_args %}` Askama loop for global flags
    - File contains `{{ version }}` template variable
    - File contains `@media` rule with `768px` breakpoint for responsive sidebar
    - File contains "Getting Started" section heading
    - File contains `logtok tokenize` and `logtok detokenize` example commands
    - File contains hamburger toggle element (button or div with class like `hamburger`)
    - File contains `<nav` element for sidebar
    - File contains no `<link rel="stylesheet"` external CSS references
    - File contains no `<script src=` external JS references
    - File contains `position: relative` on code block wrapper and `position: absolute` on copy button
  </acceptance_criteria>
  <done>
    A complete, well-structured Askama HTML template exists at templates/docs.html containing:
    (1) Dark theme CSS matching CLI colors, (2) responsive sidebar with hamburger toggle,
    (3) Getting Started, Overview, Command Reference, and Token Categories sections,
    (4) copy-to-clipboard JS with clipboard API + execCommand fallback,
    (5) Askama template variables for version, commands, args, and token categories,
    (6) all CSS and JS embedded inline with zero external dependencies.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template data -> HTML | All data comes from compiled clap tree, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information Disclosure) | Generated HTML file | accept | HTML contains only CLI help text and static content -- no secrets, no PII. All data is already public (visible via --help). |
| T-05-02 | T (Tampering) | Template file | accept | Template is compiled into binary at build time. Cannot be tampered at runtime. |
</threat_model>

<verification>
- `templates/docs.html` exists with valid HTML structure
- Template contains Askama syntax ({{ }}, {% %}) for dynamic content
- All CSS inline in `<style>` tag, all JS inline in `<script>` tag
- No external resource references (no `<link>`, no `<script src>`)
- Copy-to-clipboard JS includes both navigator.clipboard and execCommand paths
</verification>

<success_criteria>
- Template file is syntactically valid HTML with Askama template directives
- Dark theme colors match CLI palette (yellow headers, green code, cyan accents on #1a1a2e background)
- Responsive layout with sidebar visible on desktop, hamburger toggle on mobile
- All 4 content sections present with proper structure
- Copy-to-clipboard buttons on all code blocks with dual-path JS
</success_criteria>

<output>
After completion, create `.planning/phases/05-html-documentation/05-01-SUMMARY.md`
</output>
