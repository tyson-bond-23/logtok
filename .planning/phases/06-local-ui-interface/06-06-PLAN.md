---
phase: 06-local-ui-interface
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/ui/base.html
  - templates/ui/tokenize.html
  - static/styles.css
  - static/app.js
  - tailwind.config.js
autonomous: true
gap_closure: true
requirements:
  - UI-03
  - UI-04
  - UI-06
  - UI-12
  - UI-14

must_haves:
  truths:
    - "Dashboard renders fully without any internet connection (no CDN, no external fonts)"
    - "Tokenize panel has 4 input methods: paste, file upload, drag-drop, and file path text input"
    - "Recent files dropdown appears in tokenize panel populated from localStorage"
    - "Dark theme background is #0f0f10, light theme background is #fafaf5"
  artifacts:
    - path: "static/styles.css"
      provides: "Complete self-contained CSS with all Tailwind utility classes used by templates"
      contains: "#0f0f10"
    - path: "templates/ui/base.html"
      provides: "Dashboard layout with no external CDN or font references"
    - path: "templates/ui/tokenize.html"
      provides: "Tokenize panel with file_path input and recent files dropdown"
      contains: "file_path"
    - path: "static/app.js"
      provides: "Alpine.js app with selectRecentFile helper"
  key_links:
    - from: "templates/ui/tokenize.html"
      to: "static/app.js"
      via: "recentFiles array and selectRecentFile()"
      pattern: "recentFiles"
    - from: "templates/ui/base.html"
      to: "static/styles.css"
      via: "link rel=stylesheet"
      pattern: "/static/styles.css"
---

<objective>
Close 3 verification gaps from 06-VERIFICATION.md that block full phase goal achievement.

Gap 1 (BLOCKER): Remove all external CDN dependencies (Tailwind CSS CDN script, Google Fonts) from base.html. Replace with a self-contained compiled CSS file embedded via rust-embed. Use system font stack instead of Google Fonts.

Gap 2 (WARNING): Add missing file_path text input field and recent files dropdown to tokenize panel template.

Gap 3 (MINOR): Fix dark theme background from #0a0a0b to #0f0f10 and light theme background from #ffffff to #fafaf5.

Purpose: Restore single-binary offline distribution (UI-12), complete all 4 input methods (UI-06, D-18), surface recent files in UI (UI-14, D-25), and match exact color specs (UI-03, UI-04, D-10, D-11).
Output: Updated templates and CSS that pass verification with 0 gaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-local-ui-interface/06-VERIFICATION.md
@.planning/phases/06-local-ui-interface/06-CONTEXT.md
@.planning/phases/06-local-ui-interface/06-05-SUMMARY.md

<interfaces>
<!-- From static/app.js — Alpine.js app state that tokenize.html will reference -->
From static/app.js:
```javascript
recentFiles: JSON.parse(localStorage.getItem('logtok-recent') || '[]'),
addRecentFile(path) { /* adds to recentFiles, saves to localStorage */ },
removeRecentFile(path) { /* removes from recentFiles, saves to localStorage */ },
```

<!-- From templates/ui/base.html — Tailwind config and theme classes currently in use -->
Tailwind custom colors currently defined in CDN config:
- brand: { 50: '#eef2ff', 100: '#e0e7ff', 400: '#818cf8', 500: '#6366f1', 600: '#4f46e5', 700: '#4338ca', 900: '#312e81' }
- surface: { 50: '#fafafa', 100: '#f4f4f5', 200: '#e4e4e7', 700: '#18181b', 800: '#111113', 900: '#0a0a0b', 950: '#050506' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tailwind CDN with compiled self-contained CSS and fix theme colors</name>
  <files>templates/ui/base.html, static/styles.css, tailwind.config.js</files>
  <read_first>
    - templates/ui/base.html (current CDN references and Tailwind config to extract)
    - templates/ui/tokenize.html (classes used)
    - templates/ui/detokenize.html (classes used)
    - templates/ui/store.html (classes used)
    - templates/ui/config.html (classes used)
    - templates/ui/docs.html (classes used)
    - static/styles.css (current minimal overrides)
  </read_first>
  <action>
    **Step 1: Create tailwind.config.js** at project root with the custom color and font configuration currently inline in base.html, but with corrected color values:
    - surface-900: #0f0f10 (was #0a0a0b — fixes Gap 3 dark bg per D-10)
    - surface-50: #fafaf5 (was #fafafa — closer to warm white per D-11)
    - All other brand/surface colors remain the same
    - Font family: system font stack only (remove Inter, JetBrains Mono references — fixes Gap 1 Google Fonts dependency)
    - Content paths: ["./templates/ui/**/*.html"]
    - darkMode: 'class'

    ```javascript
    module.exports = {
      darkMode: 'class',
      content: ['./templates/ui/**/*.html'],
      theme: {
        extend: {
          colors: {
            brand: { 50: '#eef2ff', 100: '#e0e7ff', 400: '#818cf8', 500: '#6366f1', 600: '#4f46e5', 700: '#4338ca', 900: '#312e81' },
            surface: { 50: '#fafaf5', 100: '#f4f4f5', 200: '#e4e4e7', 700: '#18181b', 800: '#111113', 900: '#0f0f10', 950: '#050506' },
          },
          fontFamily: {
            sans: ['system-ui', '-apple-system', 'BlinkMacSystemFont', '"Segoe UI"', 'Roboto', '"Helvetica Neue"', 'sans-serif'],
            mono: ['ui-monospace', '"Cascadia Code"', '"Fira Code"', 'Menlo', 'Monaco', 'Consolas', 'monospace'],
          },
        }
      },
      plugins: [],
    }
    ```

    **Step 2: Install Tailwind CLI and compile CSS.**
    Run:
    ```bash
    npx tailwindcss -o static/tailwind.css --minify
    ```
    This scans all templates, extracts used classes, and produces a single minified CSS file containing ONLY the utility classes actually used. No runtime CDN needed.

    If npx is not available, download the Tailwind standalone CLI binary from https://github.com/tailwindlabs/tailwindcss/releases and run it directly.

    **Step 3: Merge compiled Tailwind output into static/styles.css.**
    Prepend the compiled Tailwind CSS to the existing styles.css content (which has Alpine cloak, HTMX indicator, and light-mode overrides). The final static/styles.css should be:
    1. Compiled Tailwind utilities (all classes used in templates)
    2. Alpine.js [x-cloak] rule
    3. HTMX indicator rules
    4. Light mode select option override

    **Step 4: Update templates/ui/base.html.**
    Remove these lines entirely:
    - Line 7: `<script src="https://cdn.tailwindcss.com"></script>`
    - Lines 8-23: The inline `<script>tailwind.config = { ... }</script>` block
    - Line 25: `<link rel="preconnect" href="https://fonts.googleapis.com">`
    - Line 26: `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`
    - Line 27: `<link href="https://fonts.googleapis.com/css2?family=Inter...` stylesheet link

    Keep:
    - Line 28: `<link rel="stylesheet" href="/static/styles.css">` (this now loads the complete self-contained CSS)
    - All script tags for app.js, alpine.min.js, htmx.min.js

    **Step 5: Fix light mode background class in base.html body tag.**
    Change line 41 from:
    ```
    :class="theme === 'light' ? 'light-mode bg-white text-zinc-900' : 'bg-surface-900 text-zinc-100'"
    ```
    to:
    ```
    :class="theme === 'light' ? 'light-mode bg-surface-50 text-zinc-900' : 'bg-surface-900 text-zinc-100'"
    ```
    This uses bg-surface-50 (#fafaf5) instead of bg-white (#ffffff) per D-11.

    Similarly, update the nav background for light mode from `bg-white/80` to `bg-surface-50/80`:
    ```
    :class="theme === 'light' ? 'bg-surface-50/80 border-zinc-200' : 'bg-surface-900/80 border-zinc-800'"
    ```

    **Step 6: Also fix any bg-white references in tokenize.html** — change `bg-white` to `bg-surface-50` in the tokenize form container:
    ```
    theme === 'light' ? 'border-zinc-200 bg-surface-50' : 'border-zinc-800 bg-surface-800'
    ```
  </action>
  <verify>
    <automated>grep -c "cdn.tailwindcss.com" templates/ui/base.html | grep "^0$" && grep -c "fonts.googleapis.com" templates/ui/base.html | grep "^0$" && grep -c "fonts.gstatic.com" templates/ui/base.html | grep "^0$" && grep "#0f0f10" static/styles.css && grep "fafaf5" static/styles.css && grep "bg-surface-50" templates/ui/base.html && echo "PASS: All CDN refs removed, colors correct"</automated>
  </verify>
  <acceptance_criteria>
    - templates/ui/base.html contains zero occurrences of "cdn.tailwindcss.com"
    - templates/ui/base.html contains zero occurrences of "fonts.googleapis.com"
    - templates/ui/base.html contains zero occurrences of "fonts.gstatic.com"
    - templates/ui/base.html contains zero occurrences of "Inter" font name
    - templates/ui/base.html contains `<link rel="stylesheet" href="/static/styles.css">`
    - templates/ui/base.html body :class uses "bg-surface-50" not "bg-white" for light mode
    - static/styles.css contains "#0f0f10" (the correct dark bg color)
    - static/styles.css contains "fafaf5" (the correct light bg color)
    - static/styles.css contains compiled Tailwind utility classes (e.g., ".font-bold", ".text-sm", ".rounded-lg")
    - static/styles.css size is > 5KB (compiled Tailwind output, not 13-line stub)
    - tailwind.config.js exists at project root with content paths and corrected colors
    - cargo check passes (templates still compile)
  </acceptance_criteria>
  <done>Dashboard CSS is fully self-contained with no external CDN or font dependencies. Dark bg is #0f0f10, light bg is #fafaf5. All Tailwind utility classes used in templates are present in the compiled CSS file.</done>
</task>

<task type="auto">
  <name>Task 2: Add file path input and recent files dropdown to tokenize panel</name>
  <files>templates/ui/tokenize.html, static/app.js</files>
  <read_first>
    - templates/ui/tokenize.html (current template to extend)
    - static/app.js (recentFiles array, addRecentFile, removeRecentFile functions)
    - src/ui/api_tokenize.rs (backend handler — confirm it accepts file_path field)
  </read_first>
  <action>
    **Step 1: Add file path input field to tokenize.html.**
    Insert a new input section between the textarea and the bottom action bar (before the closing `</div>` of the rounded-2xl container, after the textarea). Add a horizontal divider and file path input row:

    ```html
    <!-- File path input (D-18: 4th input method for large files -- server reads directly) -->
    <div class="flex items-center gap-2 px-5 py-3 border-t"
         :class="theme === 'light' ? 'border-zinc-100' : 'border-zinc-800/50'">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="shrink-0" :class="theme === 'light' ? 'text-zinc-400' : 'text-zinc-500'"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/></svg>
      <input type="text" name="file_path"
             class="flex-1 bg-transparent text-sm font-mono placeholder:text-zinc-500 focus:outline-none"
             :class="theme === 'light' ? 'text-zinc-800' : 'text-zinc-200'"
             placeholder="Or enter file path (best for large files)..."
             @change="if($el.value) addRecentFile($el.value)">
    </div>
    ```

    **Step 2: Add recent files dropdown below the file path input.**
    Insert immediately after the file path input div:

    ```html
    <!-- D-25/UI-14: Recent files dropdown from localStorage -->
    <div x-show="recentFiles.length > 0" x-transition
         class="px-5 py-2 border-t"
         :class="theme === 'light' ? 'border-zinc-100 bg-zinc-50/50' : 'border-zinc-800/50 bg-surface-950/30'">
      <p class="text-[11px] font-medium uppercase tracking-wider mb-1.5"
         :class="theme === 'light' ? 'text-zinc-400' : 'text-zinc-600'"
         x-text="t['recent.files'] || 'Recent files'"></p>
      <div class="flex flex-wrap gap-1.5">
        <template x-for="rf in recentFiles.slice(0, 5)" :key="rf">
          <button type="button"
                  class="inline-flex items-center gap-1 px-2 py-0.5 text-xs font-mono rounded border transition-colors"
                  :class="theme === 'light'
                    ? 'border-zinc-200 text-zinc-600 hover:border-brand-300 hover:text-brand-600 bg-white'
                    : 'border-zinc-700 text-zinc-400 hover:border-brand-500/50 hover:text-brand-400 bg-surface-800'"
                  @click="$el.closest('form').querySelector('[name=file_path]').value = rf"
                  x-text="rf.split(/[\\/]/).pop()">
          </button>
        </template>
      </div>
    </div>
    ```

    **Step 3: Add i18n key for "Recent files" label.**
    In src/ui/i18n.rs, add the translation key to both EN and HE maps:
    - EN: `("recent.files", "Recent files")`
    - HE: `("recent.files", "\u{05e7}\u{05d1}\u{05e6}\u{05d9}\u{05dd} \u{05d0}\u{05d7}\u{05e8}\u{05d5}\u{05e0}\u{05d9}\u{05dd}")`

    **Step 4: Verify app.js already has the needed functions.**
    Confirm that `addRecentFile(path)` and `recentFiles` are already defined in app.js (they are per read_first). No changes needed to app.js unless the `selectRecentFile` behavior needs to auto-submit -- the current plan just fills the input field via the @click handler in the template, which is sufficient.
  </action>
  <verify>
    <automated>grep 'name="file_path"' templates/ui/tokenize.html && grep "recentFiles" templates/ui/tokenize.html && grep "recent.files" src/ui/i18n.rs && cargo check && echo "PASS: file_path input and recent files present, compiles"</automated>
  </verify>
  <acceptance_criteria>
    - templates/ui/tokenize.html contains `<input type="text" name="file_path"`
    - templates/ui/tokenize.html contains `x-show="recentFiles.length > 0"`
    - templates/ui/tokenize.html contains `x-for="rf in recentFiles`
    - templates/ui/tokenize.html contains `@click` handler that sets file_path input value from recent file
    - src/ui/i18n.rs contains `"recent.files"` key in both EN and HE translation maps
    - cargo check passes with no new errors
  </acceptance_criteria>
  <done>Tokenize panel has all 4 input methods (paste textarea, file upload, drag-drop, file path text input). Recent files dropdown appears when localStorage has saved paths, allowing quick re-selection. Backend already supports file_path field — no Rust API changes needed.</done>
</task>

<task type="auto">
  <name>Task 3: Verify compilation and run integration tests</name>
  <files></files>
  <read_first>
    - tests/ui_integration.rs (existing integration tests to confirm they still pass)
  </read_first>
  <action>
    Run the full build and test suite to confirm all changes integrate correctly:

    1. Run `cargo check` to verify Rust compilation passes with updated templates.
    2. Run `cargo test --test ui_integration` to confirm all 9 existing integration tests pass.
    3. Run `cargo test` to confirm no regressions across the full test suite.
    4. Verify the static/styles.css file is being served correctly by checking the rust-embed StaticAssets struct includes it (it already does since static/ folder is the embed path).
    5. Verify that no external URLs remain in any template:
       ```bash
       grep -r "https://" templates/ui/ | grep -v "{#" || echo "No external URLs found"
       ```
       The result should show zero external URL references (or only comment references).
  </action>
  <verify>
    <automated>cargo test --test ui_integration 2>&1 | tail -5 && grep -rc "https://" templates/ui/ | grep ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - cargo check exits with code 0
    - cargo test --test ui_integration shows "9 passed, 0 failed" (or all tests pass)
    - cargo test (full suite) shows 0 failures
    - grep -r "https://" templates/ui/ returns no matches (zero external URLs in templates)
  </acceptance_criteria>
  <done>All gap closure changes compile, pass existing tests, and contain zero external URL references. The dashboard is fully self-contained for offline/single-binary distribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File path input -> server | User-supplied file path read by server (existing boundary, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | file_path input field | accept | Already mitigated in api_tokenize.rs (validates path is regular file, server binds 127.0.0.1 only per D-20). No new server-side changes needed — this task adds only the UI input element. |
| T-06-06-02 | Information Disclosure | recent files in localStorage | accept | File paths stored in browser localStorage on user's own machine. No network transmission. Low risk for localhost-only tool. |
</threat_model>

<verification>
1. `cargo check` passes with 0 errors
2. `cargo test --test ui_integration` — all tests pass
3. `grep -r "cdn.tailwindcss.com\|fonts.googleapis.com\|fonts.gstatic.com" templates/ui/` returns no matches
4. `grep 'name="file_path"' templates/ui/tokenize.html` returns a match
5. `grep "recentFiles" templates/ui/tokenize.html` returns matches
6. `grep "#0f0f10" static/styles.css` returns a match
7. static/styles.css file size > 5KB (compiled Tailwind, not stub)
</verification>

<success_criteria>
- Dashboard renders fully offline with no external CDN or font dependencies (UI-12 satisfied)
- Tokenize panel has 4 input methods: paste, upload, drag-drop, file path (UI-06 fully satisfied)
- Recent files dropdown appears in tokenize panel from localStorage data (UI-14 fully satisfied)
- Dark theme bg is #0f0f10, light theme bg is #fafaf5 (UI-03, UI-04 fully satisfied)
- All existing integration tests pass with no regressions
- 0 external URL references in templates
</success_criteria>

<output>
After completion, create `.planning/phases/06-local-ui-interface/06-06-SUMMARY.md`
</output>
